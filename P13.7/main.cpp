#include <iostream>
#include <map>
#include <iterator>
using namespace std;
using std::map;
using std::iterator;

typedef map<Node*, Node*> NodeMap;

Node* copy_recursive(Node* cur, NodeMap& nm){
    if(cur==NULL) return NULL;
    nm::iterator i= m,.find(cur);
    if(i!=nm.end()){
        return i->
    }
}

int main()
{
    cout << "Hello world!" << endl;
    return 0;
}
